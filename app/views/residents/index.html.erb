

    <div class = "resident_view">
      <ul>
        <% @residents.each do |resident| %>
          <li><button id ="residents" data-action="click->edit-homepage#resident" data-value="<%=resident.id%>"><%=resident.name%></button></li>
        <% end %>
      </ul>
    </div>
